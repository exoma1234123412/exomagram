---
description: Deploy a preview build to Vercel and return the preview URL
---

1. Check if Vercel CLI is installed: `which vercel`
2. If not installed, tell the user: `npm i -g vercel`
3. Run `vercel --yes` to deploy a preview
4. Report the preview URL
5. If deploy fails, read the error and suggest a fix

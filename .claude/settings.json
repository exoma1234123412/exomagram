{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "npx tsc --noEmit --pretty 2>&1 | head -5 || true",
            "timeout": 15000
          }
        ]
      },
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "violations=$(grep -rn 'text-violet-[56]00' src/ --include='*.tsx' | grep -v 'deep_work\\|CATEGORY\\|deepWork\\|Deep Work' | head -3); if [ -n \"$violations\" ]; then echo \"BLOCKED: Brand violet colors found. Use text-primary instead.\"; echo \"$violations\"; exit 2; fi",
            "timeout": 10000
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "echo 'WARNING: Destructive rm -rf detected. Proceed with caution.'; exit 0",
            "timeout": 5000
          }
        ]
      }
    ]
  }
}
